
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">A.S</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <% if(typeof title !== 'undefined' && title == "Home") { %>
            <a class="nav-link active" href="/home"><i class="fa-solid fa-house"></i> Home</a>
            <% } else { %>
            <a class="nav-link" href="/home"><i class="fa-solid fa-house"></i> Home</a>
            <% } %>
        </li>
        <li class="nav-item">
          <% if(typeof title !== 'undefined' && title == "About") { %>
            <a class="nav-link active" href="/about"><i class="fa-solid fa-info"></i> About</a>
            <% } else { %>
            <a class="nav-link" href="/about"><i class="fa-solid fa-info"></i> About</a>
            <% } %>            
        </li>
        <li class="nav-item">
          <% if(typeof title !== 'undefined' && title == "Projects") { %>
            <a class="nav-link active" href="/projects"><i class="fa-solid fa-diagram-project"></i> Projects</a>
            <% } else { %>
            <a class="nav-link" href="/projects"><i class="fa-solid fa-diagram-project"></i> Projects</a>
            <% } %>            
        </li>
        <li class="nav-item">
          <% if(typeof title !== 'undefined' && title == "Contact Us") { %>
            <a class="nav-link active" href="/contactus"><i class="fa-solid fa-phone"></i> Contact Us</a>
            <% } else { %>
            <a class="nav-link" href="/contactus"><i class="fa-solid fa-phone"></i> Contact Us</a>
            <% } %>            
        </li>
      </ul>
    </div>
  </div>
</nav>